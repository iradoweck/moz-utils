[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "moz-utils"
version = "0.3.9"
description = "Funções de utilidade para Moçambique. Validação de NUIT, documentos e formatação de telefones."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.8"
keywords = ["mozambique", "mz", "nuit", "dire", "postal-code", "cep", "m-pesa", "vodacom", "metical"]
authors = [
    {name = "Edmilson Muacigarro"}
]
maintainers = [
    {name = "Edmilson Muacigarro"}
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://iradoweck.github.io/moz-utils"
Repository = "https://github.com/iradoweck/moz-utils"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pyright]
exclude = [
    "**/__pyrefly_virtual__/**",
    "**/*.md",
    "**/*.mdx"
]
ignore = [
    "**/__pyrefly_virtual__/**",
    "**/*.md",
    "**/*.mdx"
]
